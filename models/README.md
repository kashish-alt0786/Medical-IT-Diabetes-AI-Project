# Model artifact directory

Reserved for future versioned model artifacts. The current Streamlit deployment continues to load the production artifact from the repository root as `model.pkl` to preserve the existing application contract.
