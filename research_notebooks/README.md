# Research Notebooks

This directory is reserved for exploratory notebooks, experiments, visual analysis and development drafts.

Production Streamlit code, reusable inference logic, tests and CI/CD configuration remain outside this directory.

The original `Diabetes_AI_Project.ipynb.ipynb` is intentionally retained at the repository root for now so its existing Git history and references are not broken. It can be relocated here as a separate repository-history cleanup step after verifying all external links.
